@model List<ResultRoomDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/_AdminLayout.cshtml";
}

<div class="row page-titles mx-0">
    <div class="col p-md-0">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/Dashboard/Index">Ana Sayfa</a></li>
            <li class="breadcrumb-item active"><a href="/AdminRoom/Index">Odalar</a></li>
        </ol>
    </div>
</div>

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Odalar</h4>
                    <div class="table-responsive">
                        <table class="table table-bordered table-striped verticle-middle text-center">
                            <thead>
                                <tr>
                                    <th scope="col">Oda Numarası</th>
                                    <th scope="col">Oda Adı</th>
                                    <th scope="col">Yatak Sayısı</th>
                                    <th scope="col">Banyo Sayısı</th>
                                    <th scope="col">İşlemler</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    Random random = new Random();
                                    List<string> strings = new List<string>() { "gradient-1", "gradient-2", "gradient-3", "gradient-4", "gradient-5", "gradient-6", "gradient-7", "gradient-8", "gradient-9" };
                                    int randomIndex = random.Next(strings.Count);
                                    string randomString = strings[randomIndex];

                                    <tr>
                                        <td>@item.RoomNumber</td>
                                        <td>
                                            <span class="label @randomString btn-rounded">@item.Title</span>
                                        </td>
                                        <td>@item.BedCount</td>
                                        <td>@item.BathCount</td>
                                        <td>
                                            <span>
                                                <a href="/AdminRoom/UpdateRoom/@item.RoomID" data-toggle="tooltip" data-placement="top" title="Güncelle"><i class="fa fa-pencil color-muted m-r-5"></i></a>
                                                <a href="/AdminRoom/DeleteRoom/@item.RoomID" data-toggle="tooltip" data-placement="top" title="Sil"><i class="fa fa-close color-danger"></i></a>
                                            </span>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <a href="/AdminRoom/AddRoom/" class="btn btn-outline-primary col-12">Yeni Oda Ekle!</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

